# Trivy ignore list — accepted CVEs
# https://trivy.dev/docs/configuration/filtering/#trivyignore
#
# Format (one CVE per line):
#   CVE-YYYY-NNNNN    # justification (expires: YYYY-MM-DD)
#
# Only the repository owner decides what to exclude and for how long.
